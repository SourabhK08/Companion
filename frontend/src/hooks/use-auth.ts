/**
 * Re-export useAuth hook for convenient imports.
 *
 * Consumers can import from "@/hooks/use-auth" instead of
 * reaching into "@/lib/auth/auth-context".
 */

export { useAuth } from "@/lib/auth/auth-context";
